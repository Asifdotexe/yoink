services:
  - type: web
    name: yoinky
    runtime: python
    buildCommand: pip install --upgrade pip && pip install .
    startCommand: uvicorn yoink.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
